name: test-chrome

on: [workflow_dispatch]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-latest ]
    steps:
      - uses: browser-actions/setup-chrome@v1
      - name: Run chrome
        run: |
          chrome --version
